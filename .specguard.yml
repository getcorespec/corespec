model: anthropic/claude-haiku-4-5-20251001
threshold: 0.7

ignore:
  # Lockfiles — auto-generated, no behavior
  - pnpm-lock.yaml
  # Documentation — prose, not code
  - "*.md"
  # Config files — declarative, not behavioral
  - "*.yml"
  - "*.yaml"
  - "*.json"
  # Build output and type declarations
  - "dist/**"
  - "*.d.ts"
  # License and legal
  - LICENSE
