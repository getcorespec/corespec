# Ideas

## `specguard init`

Interactive setup command that scaffolds specguard into a project:

- Generate `.github/workflows/specguard.yml` workflow file
- Generate `.specguard.yml` config with sensible defaults
- Detect existing spec framework and pre-configure
- Prompt for LLM provider choice and remind to set secrets
- Validate spec format (check that existing specs follow expected structure)
